package com.java;

public class rectngles {
	    public static void main(String[] args) {
	        rectngle rec = new rectngle();
	        rec.set_length();
	        rec.set_width();
	        System.out.println("Length: "+rec.get_length()+" Width: "+rec.get_width()+" Perimeter: "+rec.perimeter()+" Area: "+rec.area());
	    }
	    
	}

